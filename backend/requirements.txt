fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.4
pydantic==2.9.2
pydantic-settings==2.5.2
anthropic==0.36.0
openai==1.54.4
# Pinned: openai 1.54.x passes `proxies=` to httpx.Client, which httpx 0.28 removed.
# Unpinned, pip resolves httpx>=0.28 and every embedding call fails with
# "Client.__init__() got an unexpected keyword argument 'proxies'".
# requirements-dev.txt already pins the same version.
httpx==0.27.2
pypdf==5.1.0
python-dotenv==1.0.1
pyyaml==6.0.2
